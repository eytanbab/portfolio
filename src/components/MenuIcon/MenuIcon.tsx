import './style.css';
const MenuIcon = () => {
  return (
    <input
      type='checkbox'
      role='button'
      aria-label='Display the menu'
      className='menu'

    />
  );
};

export default MenuIcon;
